class Log {
  constructor(id, name, date, paymentType, paidWith) {
    this.id = id;
    this.name = name;
    this.paymentType = paymentType;
    this.date = date;
    this.paidWith = paidWith;
    // this.price = price;
  }
}

export default Log;
